
<div class="container">

<div class="list-user">
  <h1 class = "text-center text-danger margin_bottom_10" >Danh sách người dùng</h1>

  <a href="<%=new_admin_user_path%>" class="btn btn-primary margin_bottom_10"  >Thêm người dùng</a>

<table class="table table-hover table-bordered" >
  <tr style="background: pink">
    <th>STT</th>
    <th>Email</th>
    <th>Tên</th>
    <th>Số điện thoại</th>
    <th>Sinh nhật</th>
    <th>Địa chỉ</th>
    <th>Chức năng</th>
  </tr>
  <% @users.each_with_index do |user,index| %>
  <tr class="box-user">
    <td><%= index + 1 + (@page-1)*3  %></td>
    <td><%= user.email %></td>
    <td><%= user.name %></td>
    <td><%= user.phone %></td>
    <td><%= user.birthday.strftime("%d/%m/%Y") %></td>
    <td><%= user.address %>, <%= user.district.name %>, <%=user.district.city.name%></td>
    <td>
      <a href="<%= admin_user_path(user)%>" class="btn btn-primary">Xem</a>
      <a href="<%= edit_admin_user_path(user) %>" class="btn btn-warning">Sửa</a>
      <a href ="<%= admin_user_path(user) %>" data-method="delete" data-target="<%= user.id %>" class="btn btn-danger delete-user" data-confirm="Bạn chắc chắn muốn xóa?">Xóa</a>
    </td>
  </tr>

  <% end %>
</table>
<div class="pagination pull-right">
  <%= will_paginate @users , renderer: BootstrapPagination::Rails %>
</div>
</div>
</div>
